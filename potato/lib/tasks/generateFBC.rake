desc "I am a task that generates application specific resources. Models, Migrations, DB schema, etc, etc"
task :generateFBC do
puts "generating models as resources..."
## get last global id()
## request item by id


## alternative, use covalent api to fetch all

end
