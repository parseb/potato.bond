class GlobalState < ApplicationRecord
    # def self.increment_g
    #         if GlobalState.count == 0 then GlobalState.create!(gid:"0") end
    #         g= GlobalState.last
    #         g.gid = (GlobalState.count + 1).to_s
    #         g.save!
    # end
end
