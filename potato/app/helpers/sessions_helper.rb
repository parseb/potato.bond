module SessionsHelper

    def fetchAndUpdateAll(_start, _end)
        puts("#{_start} ------ fech and update all ------- #{_end}")
        return true
    end
        # do covalent fetch and push to db @todo
end
